// propertee2-java — Java 25 virtual-thread cooperative runtime for ProperTee.
// Single root project for now; design §10 envisions a core/cli split, deferred until
// there is a runnable CLI (PB/PC). The conformance fixtures already live at the Gradle
// default test-resources path (src/test/resources/tests), so no relayout is needed.
rootProject.name = 'propertee2'
